import 'package:flutter/material.dart';

class MascaraTools extends StatelessWidget{
  Widget build(context){
    return Scaffold(
      appBar: AppBar(
        title: Text('Applying Mascara with Natural eyelashes'),
      ),
      body: Container(),
    );
  }
}